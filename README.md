# gpt
Lightweight GPT

Uses Andrej Karpathy's tutorial as a guide to create a lightweight GPT that trains on Shakespeare corpus. 

# install dependency
pip3 install -r requirements.txt

# Run
python3 gpt.py